import React from "react";
import { Text, View, ActivityIndicator } from "react-native";

const Loader = () => {
  return (
    <View>
      <ActivityIndicator />
    </View>
  );
};

export default Loader;
