import React from "react";
import { Text, View } from "react-native";

const FindLocker = ({ params }) => (
  <View>
    <Text>This is the Find Locker Screen</Text>
  </View>
);

export default FindLocker;
